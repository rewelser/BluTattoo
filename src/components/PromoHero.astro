---
interface Props {
  image: string;
  alt?: string;
  className?: string;
}

const {
  image,
  alt = "",
  className = ""
} = Astro.props;
---

<section class={`relative overflow-hidden shadow-lg ${className}`}>
  <!-- Image -->
  <!-- <img
    src={image}
    alt={alt}
    loading="eager"
    decoding="async"
    class="h-[40vh] w-full object-cover md:h-[55vh]"
  /> -->

  <!-- <img
  src={image}
  alt={alt}
  loading="eager"
  decoding="async"
  class="w-full h-auto"
  style="object-fit: contain;"
  /> -->

  <!-- <img
  src={image}
  alt={alt}
  class="w-full h-[55vh] object-contain bg-black"
  loading="eager"
  decoding="async"
  /> -->

  <img
  src={image}
  alt={alt}
  class="w-full h-auto"
  loading="eager"
  decoding="async"
/>


  <!-- gradient overlay -->
  <div class="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/70 via-black/30 to-transparent"></div>
</section>
