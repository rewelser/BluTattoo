---
import {frameRegistry, type FrameKey} from "./decor/frameportraits/frameclips/registry.ts";
import SparkleCorners from "./decor/SparkleCorners.astro";
import type {ImageMetadata} from "astro";
import {defaults} from "../config/site.ts";

import GuestArtistCardInfoBanner from "./decor/GuestArtistCardInfoBanner.astro";

interface Props {
    artistPhoto: ImageMetadata | undefined;
    artistName: string;
    photoAlt: string;
    frameType?: FrameKey;
    isGuestSpot?: boolean;
    class?: string;
}

const {
    artistPhoto,
    artistName,
    photoAlt,
    frameType,
    isGuestSpot,
    class: className = "",
} = Astro.props;

const config = frameType ? frameRegistry[frameType] : undefined;

// const config = frameRegistry[frameType];
//
// if (!config) {
//     throw new Error(`Unknown frame key: ${frameType}`);
// }
//
// const {FrameClipComponent, frame, mask} = config;

const defaultPersonRunwayPhoto = defaults.personRunwayPhoto;
---
<>
    {!config ?
        (
                <>
                    <div class="relative">
                        <div
                                class={`artist-card group relative overflow-hidden rounded-2xl ${className}`}
                        >
                            <img
                                    src={artistPhoto?.src ?? defaultPersonRunwayPhoto}
                                    alt={photoAlt}
                                    data-artist-card-photo
                                    loading="lazy"
                                    decoding="async"
                                    class="object-fit transition-transform duration-300 group-hover:scale-105"
                            />
                        </div>
                        <SparkleCorners
                                color="black"
                                fillColor="gold"
                                size={24}
                                gap={16}
                                offset={-20}
                        />
                    </div>
                    <p class="mt-3 text-center text-base font-medium tracking-wide">
                        {artistName}
                    </p>

                </>
        ) : (
                    <>
                        <GuestArtistCardInfoBanner/>
                        <div
                                class={`framed-artist-card group relative overflow-visible rounded-2xl ${className}`}
                        >
                            <div
                                    class="applied-mask--frameclip h-full w-full"
                                    style={`mask-image: url("${config.mask.src}"); -webkit-mask-image: url("${config.mask.src}");`}
                            >ffffffff
                                <img
                                        src={artistPhoto?.src ?? defaultPersonRunwayPhoto}
                                        alt={photoAlt}
                                        data-artist-card-photo
                                        loading="lazy"
                                        decoding="async"
                                        class="absolute object-cover transition-transform duration-300 group-hover:scale-105"
                                />
                            </div>

                            <img
                                    src={config.frame.src}
                                    alt=""
                                    data-frame
                                    loading="lazy"
                                    decoding="async"
                                    class="absolute inset-0 h-full w-full pointer-events-none"
                                    aria-hidden="true"
                            />
                            <config.FrameClipComponent/>
                            <SparkleCorners
                                    color="black"
                                    fillColor="gold"
                                    size={24}
                                    gap={16}
                                    offset={-40}
                            />

                            <script is:inline>
                                (() => {
                                    const root = document.currentScript.parentElement;
                                    const shape = root.querySelector("[data-frame-window]");
                                    const photo = root.querySelector("[data-artist-card-photo]");

                                    function syncPhotoToWindow() {
                                        const shapeRect = shape.getBoundingClientRect();
                                        const wrapperRect = root.getBoundingClientRect();

                                        photo.style.top = `${shapeRect.top - wrapperRect.top}px`;
                                        photo.style.left = `${shapeRect.left - wrapperRect.left}px`;
                                        photo.style.width = `${shapeRect.width}px`;
                                        photo.style.height = `${shapeRect.height}px`;
                                    }

                                    const ro = new ResizeObserver(syncPhotoToWindow);
                                    ro.observe(root);

                                    if (photo.complete) {
                                        syncPhotoToWindow();
                                    } else {
                                        photo.addEventListener("load", syncPhotoToWindow, {once: true});
                                    }

                                    window.addEventListener("load", syncPhotoToWindow, {once: true});
                                })()
                            </script>
                        </div>
                        <p class="mt-3 text-center text-base font-medium tracking-wide">
                            {artistName}
                        </p>
                    </>

        )
    }
</>