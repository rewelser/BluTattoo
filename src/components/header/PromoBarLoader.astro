---
import PromoBar from "./PromoBar.astro";
import { loadPromoCandidateEvents, loadEventsPublished, pickPromoEvent } from "../../scripts/events";

const {
  fallbackHref = "/events",
  enabled = true,
} = Astro.props as {
  fallbackHref?: string;
  enabled?: boolean;
};
if (!enabled) return;

const events = await loadPromoCandidateEvents(new Date());
const promoEvent = events.find((ev) => ev.featured) ?? events[0] ?? null;

if (!promoEvent?.promoBar?.message) return;

const storageKey = `bt_promo_dismissed_${promoEvent.id}`;
const href = promoEvent.promoBar.href ?? fallbackHref;
const { message, color1, color2 } = promoEvent.promoBar;
---

<PromoBar
  storageKey={storageKey}
  message={message}
  href={href}
  color1={color1}
  color2={color2}
/>