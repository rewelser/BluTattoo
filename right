  - name: "siteInfo"
    label: "Site Info"
    files:
      - name: "main"
        label: "Main"
        file: "src/content/siteInfo/main.json"
        format: "json"           # ← add this
        fields:
          - { name: address, label: Address, widget: text }
          - { name: phone, label: Phone, widget: string, required: false }

          - label: Hours
            name: hours
            widget: list
            required: false
            fields:
              - { name: label, label: Label, widget: string }
              - { name: value, label: Value, widget: string }

          - label: Promo Banner
            name: promoBanner
            widget: object
            required: false
            fields:
              - { name: enabled, label: Enabled, widget: boolean, default: false }
              - { name: text, label: Text, widget: string }
              - { name: url, label: Link URL, widget: string, required: false }

          # NEW: Branding section (background + logo)
          - label: Branding
            name: branding
            widget: object
            required: false
            # collapsed: true     # ← comment out while testing
            fields:
              - label: Background Image
                name: background
                widget: image
                required: false
                hint: "Large photo used behind the content (jpg/png)."
                media_folder: "public/uploads/misc_images"
                public_folder: "/uploads/misc_images"

              - label: Logo (SVG or PNG)
                name: logo
                widget: image
                required: false
                hint: "Main site logo used in the header."
                media_folder: "public/uploads/misc_images"
                public_folder: "/uploads/misc_images"
